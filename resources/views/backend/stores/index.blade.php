@extends('backend.layouts.app')
@section('title','Cửa hàng')
@section('content')
<div class="app-content-header"><div class="container-fluid"><h1 class="mb-0">Cửa hàng</h1></div></div>
<div class="app-content"><div class="container-fluid">@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif<div class="card"><div class="card-header d-flex justify-content-between align-items-center"><h3 class="card-title mb-0">Danh sách cửa hàng</h3><a href="{{ route('backend.stores.create') }}" class="btn btn-primary btn-sm ms-auto">Thêm mới</a></div><div class="card-body"><div class="table-responsive"><table class="table table-striped table-hover align-middle mb-0"><thead><tr><th>ID</th><th>Mã</th><th>Tên</th><th>Tỉnh/TP</th><th>Giờ</th><th>Trạng thái</th><th>Thao tác</th></tr></thead><tbody>@forelse($stores as $store)<tr><td>{{ $store->id }}</td><td>{{ $store->code }}</td><td>{{ $store->name }}</td><td>{{ $store->province }}</td><td>{{ $store->open_time }} - {{ $store->close_time }}</td><td>{{ $store->status }}</td><td><a href="{{ route('backend.stores.show',$store) }}" class="btn btn-info btn-sm text-white">Xem</a> <a href="{{ route('backend.stores.edit',$store) }}" class="btn btn-outline-primary btn-sm">Sửa</a> <form action="{{ route('backend.stores.destroy',$store) }}" method="POST" class="d-inline" onsubmit="return confirm('Xóa cửa hàng này?')">@csrf @method('DELETE')<button class="btn btn-outline-danger btn-sm" type="submit">Xóa</button></form></td></tr>@empty<tr><td colspan="7" class="text-center">Chưa có dữ liệu.</td></tr>@endforelse</tbody></table></div><div class="mt-3">{{ $stores->links() }}</div></div></div></div></div>
@endsection
