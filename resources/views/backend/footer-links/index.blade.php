@extends('backend.layouts.app')
@section('title','Link chân trang')
@section('content')
<div class="app-content-header"><div class="container-fluid"><h1 class="mb-0">Link chân trang</h1></div></div>
<div class="app-content"><div class="container-fluid">@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif<div class="card"><div class="card-header d-flex justify-content-between align-items-center"><h3 class="card-title mb-0">Danh sách footer links</h3><a href="{{ route('backend.footer-links.create') }}" class="btn btn-primary btn-sm ms-auto">Thêm mới</a></div><div class="card-body"><table class="table table-striped table-hover align-middle mb-0"><thead><tr><th>ID</th><th>Nhóm</th><th>Tiêu đề</th><th>URL</th><th>Sort</th><th>Active</th><th>Thao tác</th></tr></thead><tbody>@forelse($footerLinks as $link)<tr><td>{{ $link->id }}</td><td>{{ $link->group_name }}</td><td>{{ $link->title }}</td><td>{{ $link->url }}</td><td>{{ $link->sort_order }}</td><td>{{ $link->is_active ? 'Yes' : 'No' }}</td><td><a href="{{ route('backend.footer-links.show', $link) }}" class="btn btn-info btn-sm text-white">Xem</a> <a href="{{ route('backend.footer-links.edit', $link) }}" class="btn btn-outline-primary btn-sm">Sửa</a> <form method="POST" action="{{ route('backend.footer-links.destroy', $link) }}" class="d-inline" onsubmit="return confirm('Xóa link này?')">@csrf @method('DELETE')<button type="submit" class="btn btn-outline-danger btn-sm">Xóa</button></form></td></tr>@empty<tr><td colspan="7" class="text-center">Chưa có dữ liệu.</td></tr>@endforelse</tbody></table></div></div></div></div>
@endsection
